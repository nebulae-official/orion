---
description: Multi-stage builds, non-root, health checks
globs: "**/Dockerfile,**/docker-compose*.yml"
alwaysApply: false
---

# Docker

- Multi-stage builds; minimal final image.
- Run as non-root user.
- HEALTHCHECK using /health or /ready.
- Pin base image tags (e.g. python:3.14-slim).
