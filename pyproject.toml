[project]
name = "orion"
version = "0.1.0"
requires-python = ">=3.12"
description = "Project Orion monorepo"

[dependency-groups]
docs = ["zensical>=0.0.26"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = []
